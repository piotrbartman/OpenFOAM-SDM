language: bash

before_script:
    - sudo apt-get install --no-install-recommends libboost-dev libopenmpi-dev clang-3.9
    - sudo mv /usr/local/clang-5.0.0/bin/clang++ /usr/local/clang-5.0.0/bin/clang++.off
    - sudo mv /usr/local/clang-5.0.0/bin/clang /usr/local/clang-5.0.0/bin/clang.off
    - sudo update-alternatives --install /usr/bin/clang++ clang++ /usr/lib/llvm-3.9/bin/clang++ 100
    - sudo update-alternatives --install /usr/bin/clang clang /usr/lib/llvm-3.9/bin/clang 100

    - mv OpenFOAM-dev OpenFOAM-new
    - git clone --depth 1 https://github.com/OpenFOAM/OpenFOAM-dev.git
    - cp -R OpenFOAM-new/* OpenFOAM-dev
    
script:
    - cd OpenFOAM-dev
    - source etc/bashrc 
    - export WM_COMPILER=Clang
    - export WM_COMPILE_OPTION=FastCompilation
    - export MAKEFLAGS='-j3'

    - cd wmake/src 
    - make
    - cd ../..
    
    - cd src/OSspecific/POSIX/
    - ./Allwmake
    - cd ../../..
    
    - cd src
    - cd Pstream
    - wmake -j3 libso dummy
    - cd ..
    - wmake -j3 libso OpenFOAM
    - wmake -j3 libso fileFormats
    - wmake -j3 libso surfMesh
    - wmake -j3 libso triSurface
    - wmake -j3 libso meshTools
    - wmake -j3 libso finiteVolume
    - cd mesh
    - wmake -j3 libso extrudeModel
    - cd ..
    - wmake -j3 libso dynamicMesh
    - wmake -j3 libso dynamicFvMesh
    - wmake -j3 libso conversion
    - cd regionModels
    - wmake -j3 libso regionModel
    - cd ..
    - cd transportModels
    - wmake -j3 libso compressible
    - wmake -j3 libso twoPhaseMixture
    - wmake -j3 libso incompressible
    - cd ..    
    - cd thermophysicalModels
    - wmake -j3 libso specie
    - wmake -j3 libso SLGThermo
    - wmake -j3 libso thermophysicalProperties
    - wmake -j3 libso basic
    - wmake -j3 libso solidSpecie
    - wmake -j3 libso reactionThermo
    - wmake -j3 libso solidThermo
    - cd ..
    - cd lagrangian
    - wmake -j3 libso distributionModels
    - cd ..
    - cd regionModels
    - wmake -j3 libso surfaceFilmModels
    - cd ..
    - cd lagrangian
    - wmake -j3 libso basic
    - wmake -j3 libso solidParticle
    - cd ..
    - wmake -j3 libso sampling
    - cd TurbulenceModels
    - wmake -j3 libso turbulenceModels
    - wmake -j3 libso incompressible
    - wmake -j3 libso compressible
    - cd ..
    - wmake -j3 libso fvOptions
    - wmake -j3 libso radiationModels
    - cd lagrangian
    - wmake -j3 libso intermediate
    - wmake -j3 libso turbulence
    - cd ..
    - wmake -j3 libso topoChangerFvMesh
    - cd ..

    - cd applications/solvers/lagrangian/DPMFoam 
    - ./Allwmake
    - cd ../../../..
